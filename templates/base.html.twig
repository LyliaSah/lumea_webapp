<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        {# {{ encore_entry_link_tags('app') }} #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-..." crossorigin="anonymous">
        <!-- Bootstrap JS Bundle (avec Popper inclus) -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% endblock %}

{# Début du header #}
<div class="container mt-4">

    {% if app.user and app.user.prenom is defined %}
        <h1 class="text-center">Bienvenue {{ app.user.prenom|capitalize }} sur Lumea 🌟</h1>
    {% else %}
        <h1>Bienvenue sur Lumea 🌟</h1>
    {% endif %}

    <div class="row align-items-center my-4">
        <div class="col-md-5">
            <img src="{{ asset('images/logoT.svg') }}" class="img-fluid" alt="Logo Lumea">
            <p>Faciliter l'éducation à la vie affective, reationnelle et sexuelle des jeunes</p>
        </div>
        <div class="col-md-7">
            <ul class="nav justify-content-end">
                <li class="nav-item"><a class="nav-link active" href="{{ path('app_lumea') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Notre équipe</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Nos valeurs - nos missions</a></li>
                <li class="nav-item"><a class="nav-link" href="#">Nous contacter</a></li>
                {% if app.user %}
                <li class="nav-item">
                    <form action="{{ path('app_logout') }}" method="post">
                        <button class="btn btn-canard" type="submit">Déconnexion</button>
                    </form>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>


{# fin du header #}



<style>
.btn-canard {
    background-color: #008080 !important;
    color: white !important;
    border-color: #008080 !important;
}

.btn-canard:hover {
    background-color: #006666 !important;
    color: white !important;
    border-color: #006666 !important;
}
</style>


</head>
<body>

<!-- autres liens -->
</nav>

{% block body %}{% endblock %}

{# Début du footer #}
<footer style="background-color:#f3f4f6;" class="py-4 mt-5 border-top text-dark">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 text-center text-md-start mb-3 mb-md-0">
                <img src="{{ asset('images/logoT.svg') }}" alt="Logo Lumea" height="40" class="mb-2">
                <p class="mb-0 small">
                    Lumea – Pour une éducation affective, relationnelle et sexuelle accessible 🌟
                </p>
            </div>
            <div class="col-md-6 text-center text-md-end">
                <ul class="list-inline mb-2">
                    <li class="list-inline-item"><a href="#" class="text-dark text-decoration-none">Mentions légales</a></li>
                    <li class="list-inline-item"><a href="#" class="text-dark text-decoration-none">CGU</a></li>
                    <li class="list-inline-item"><a href="#" class="text-dark text-decoration-none">Confidentialité</a></li>
                    <li class="list-inline-item"><a href="#" class="text-dark text-decoration-none">Contact</a></li>
                </ul>
                <small class="text-muted">&copy; {{ "now"|date("Y") }} Lumea. Tous droits réservés.</small>
            </div>
        </div>
    </div>
</footer>
{# Fin du footer #}





{% block javascripts %}
    {# {{ encore_entry_script_tags('app') }} #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-..." crossorigin="anonymous"></script>
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}
</body>
</html>
